[project]
name = "dodgeball-2d"
version = "0.1.0"
description = "Turn-based dodgeball simulation on a 2D grid"
readme = "dodgeball_prompt.md"
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
